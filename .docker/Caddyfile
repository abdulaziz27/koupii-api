{
  admin off
  auto_https off
  order php_server before file_server
}

:80 {
  root * /app/public
  encode zstd gzip

  # Handle all requests with PHP first
  php_server

  # Fallback to static files
  file_server
}
